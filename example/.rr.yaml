version: "3"

server:
  command: "php worker.php"
  relay: pipes

http:
  address: 127.0.0.1:8080
  middleware: ["gzip"]
  pool:
    num_workers: 2
    max_jobs: 0
    allocate_timeout: 60s
    destroy_timeout: 60s

logs:
  mode: development
  level: info
