APP_NAME="{{APP_NAME}}"
APP_ENV=development
DEBUG=true
PORT=8003
SERVER_PORT=8003

; JWT (optional — falls back to APP_KEY). Prefer a dedicated secret in production.
; JWT_SECRET=
; JWT_TTL=3600
; JWT_ALG=HS256
; JWT_ISSUER=
; JWT_AUDIENCE=
